import { Placeholder } from '../Placeholder';

// VelocityChart: card-backlog-velocity > card-split の右半分に配置される
// ベロシティ可視化。親 bento-card の内部要素のため、自身は bento-card クラスを持たない。
// 0059b で実装差し替え予定。
export function VelocityChart() {
  return <Placeholder name="VelocityChart" className="velocity-chart" />;
}
